import 'package:flutter/material.dart';

const TextStyle inputStyle = TextStyle(
  fontSize: 16.0,
);

const TextStyle buttonStyle = TextStyle(
  fontSize: 16.0,
  color: Colors.white,
);

const BoxDecoration buttonDecoration = BoxDecoration(
  color: Colors.blue,
  borderRadius: BorderRadius.all(Radius.circular(5.0)),
);

const BoxDecoration outputDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.all(Radius.circular(5.0)),
  border: Border.all(color: Colors.grey),
);